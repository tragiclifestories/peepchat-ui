<input
  value={{value}}
  type={{type}}
  class="{{if value
    (concat
      "validate"
      " "
      (if _errorMessages "invalid" "valid")
    )}}"
  onkeyup={{action (mut value) value='target.value'}}
  onchange={{action (mut value) value='target.value'}}>
<label data-error={{_errorMessages}}>
  {{label}}{{yield}}
</label>